# Sensors (Placeholder)

This folder is reserved for future enterprise-grade sensor components (e.g., host/network sensors).

Current implementation lives in `network_capture/` for the initial packet-capture foundation.

