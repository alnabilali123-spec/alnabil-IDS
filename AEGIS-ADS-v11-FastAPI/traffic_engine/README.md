# Traffic Engine (Placeholder)

This folder is reserved for future real-time pipeline orchestration:
- packet capture → flow builder → feature extraction → (later) model inference → alerts

Current foundation layer (packet capture + queue) is in `network_capture/`.

